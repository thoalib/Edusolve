{
  "name": "ehms",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @ehms/web",
    "dev:api": "npm run dev --workspace @ehms/api",
    "build": "echo 'No build step required for API'",
    "build:web": "npm run build --workspace @ehms/web",
    "start:api": "npm run start --workspace @ehms/api",
    "start:web": "npm run start --workspace @ehms/web"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
